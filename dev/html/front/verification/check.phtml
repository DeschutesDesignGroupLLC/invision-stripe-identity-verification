<ips:template parameters="$member, $size=null" />
{{if $member->verified}}
    <span style="color: {setting='stripeverification_icon_color'}; margin: 0px; padding: 0px; background: none;">
        <i style="margin: 2px; {{if $size}}font-size: {$size}px;{{endif}}" class="{setting='stripeverification_icon'}"></i>
    </span>
{{endif}}
